version: "3.9"

services:
    postgres:
      image: postgres:16
      container_name: authGestiordeOpiniones
      restart: always
      environment:
        POSTGRES_DB: GestordeOpiniones
        POSTGRES_USER: postgres
        POSTGRES_PASSWORD: admin
      ports:
        - "5437:5432"
      volumes:
        - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: